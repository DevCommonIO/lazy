-- java-dracula → Dracula palette (popular JetBrains/Eclipse plugin theme)
-- Bundled theme in darcula-dark.nvim.
require("lazy").load({ plugins = { "darcula-dark.nvim" } })
require("darcula").setup({ theme = "dracula" })
