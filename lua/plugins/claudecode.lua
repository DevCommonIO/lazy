return {
  "coder/claudecode.nvim",
  opts = {
    diff_opts = {
      open_in_new_tab = false,     -- diff opens in the current tab, main buffer area
      keep_terminal_focus = false, -- focus moves to the diff
    },
  },
}
